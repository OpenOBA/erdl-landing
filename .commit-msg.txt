docs: fix spec filename drift (erdl-spec -> erdl-language-spec-v2.1)

The spec files were renamed to erdl-language-spec-v2.1.{md,en.md} but several
references still pointed at the old erdl-spec.{md,en.md} names. Fix all live
references:

- package.json `files` — the spec files were silently missing from npm packs.
- API.md / DEVELOPMENT.md / CONTRIBUTING.md — spec links.
- README.md / README.zh-CN.md — spec links + verified-vector count 317 -> 318.
- REGISTRY.md / REGISTRY.en.md — frozen-enum references.
- CHANGELOG.md / CHANGELOG.zh-CN.md — the live "version semantics" reference.

Historical CHANGELOG entries keep the old filename (they record what the file
was named at that release).
